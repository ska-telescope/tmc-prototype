[metadata]
description-file = README.rst

[aliases]
# Define `python setup.py test`
test=pytest

[coverage:run]
branch = True
source = subarraynode
data_file = subarraynode_coverage
debug = dataio,dataop

[tool:pytest]
testpaths = test

addopts =
          --forked
          --json-report
          --json-report-file=../reports/subarraynode-report.json
          --cov-branch
          --cov-report term
          --cov-report html:../reports/subarraynode_htmlcov
          --cov-report xml:../reports/subarraynode-code-coverage.xml
          --cov=subarraynode
          --junitxml=../reports/subarraynode-unit-tests.xml
          --verbose
          --dist loadscope --numprocesses auto

filterwarnings =
    ignore::DeprecationWarning
