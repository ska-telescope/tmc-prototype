apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tmc-mid.fullname" . }}-test-dishmaster"
  labels:
    {{- include "tmc-mid.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: {{- include "tmc-mid.labels" . | nindent 4 }}
      command: ['wget']
      args: ['{{ include "tmc-mid.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
