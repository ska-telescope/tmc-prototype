apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tmc-mid.fullname" . }}-test-connection"
  labels:
    {{- include "tmc-mid.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tmc-mid.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
