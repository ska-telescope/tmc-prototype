apiVersion: v1
kind: Service
metadata:
  name: {{ include "tmc-common.fullname" . }}
  labels:
    {{- include "tmc-common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tmc-common.selectorLabels" . | nindent 4 }}
