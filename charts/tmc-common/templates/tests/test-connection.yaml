apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tmc-common.fullname" . }}-test-connection"
  labels:
    {{- include "tmc-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tmc-common.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
