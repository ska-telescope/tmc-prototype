apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tmc-low.fullname" . }}-test-connection"
  labels:
    {{- include "tmc-low.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tmc-low.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
